# frontend/.env.development
# Dev-only switches. Safe to commit.
# When running `npm run dev`, this enables frontend-only auth mocks so the app
# works without a running API. Set to 0 to hit a real backend.
VITE_AUTH_MOCK=1

# Optional: point dev frontend at a remote API. Leave empty for same-origin.
# VITE_API_BASE=https://your-app.azurewebsites.net
