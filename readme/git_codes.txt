git status
cd C:\Users\david\Documents\cursus
git add .
git commit -m "lcsd dashboard misc"
git push origin +main:main

az webapp show `
  --resource-group cursus-rg `
  --name cursus-app `
  --query defaultHostName -o tsv

az deployment group what-if -g cursus-rg `
  --template-file infra/main.bicep `
  --parameters planSkuName=S1 timeout=1800

az role assignment create `
  --assignee  $AZURE_CLIENT_ID `
  --role      "Owner" `
  --scope     /subscriptions/$AZURE_SUBSCRIPTION_ID/resourceGroups/cursus-rg

az role definition list --name "DocumentDB Account Contributor" `
  --query "[].{Name:roleName, Id:id}"

az cosmosdb sql role definition list `
  --account-name <ACCOUNT> -g <RG> `
  --query "[].{Name:roleName, Id:id}"

az webapp log config -g $RG -n $APP --application-logging filesystem `
    --detailed-error-messages true --level information
